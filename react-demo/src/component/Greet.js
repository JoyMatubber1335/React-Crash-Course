export const Name = () => {
  // use export so use same name (Name) as a component
  return <h1>Hi Joy</h1>;
};
const age = () => {
  //  use default   export so use any name as  a component
  return <h1>My age is 23</h1>;
};
export default age;
